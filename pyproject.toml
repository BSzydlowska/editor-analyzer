[project]
name = "editor-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "customtkinter>=5.2.2",
    "imageio-ffmpeg>=0.6.0",
    "librosa>=0.11.0",
    "pyaaf2>=1.7.1",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
    "pyinstaller>=6.0",
    "pytest>=8.0",
]
